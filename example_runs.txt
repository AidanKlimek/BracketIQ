# Quick power rankings (Torvik only, ~5 seconds)
python main.py rank

# Detailed single team report with ESPN data
python main.py score "Duke"

# Head-to-head matchup analysis
python main.py matchup "Duke" "Houston"

# Export rankings to CSV
python main.py export

# Full rankings with ESPN for top 68 (~35 min first run, cached after)
python main.py rank --full
python main.py export --full